# ── Visão Computacional (ONNX Runtime + InsightFace) ─────────
# CPU build by default. For NVIDIA hosts swap onnxruntime → onnxruntime-gpu.
onnxruntime>=1.17.0
insightface>=0.7.3
opencv-python>=4.8.0
numpy>=1.24.0,<2.0.0

# ── Web / API ─────────────────────────────────────────────────
flask>=3.0.0
requests>=2.31.0

# ── PostgreSQL (cloud master) ────────────────────────────────
# Direct SQL access to the cloud master (replaces the Supabase REST client).
psycopg[binary]>=3.1.0

# ── Utilitários ───────────────────────────────────────────────
python-dotenv>=1.0.0
pillow>=10.0.0

# ── WhatsApp QR Code (setup_whatsapp.py) ─────────────────────
qrcode>=7.4.2

# ── Twilio SMS (opcional — apenas se USE_TWILIO=true) ─────────
# twilio>=8.0.0
